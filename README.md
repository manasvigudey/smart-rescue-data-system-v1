# Smart Rescue Data System — v1 (C)

Pure-C sensor data system: struct-based sensor storage, threshold detection, sorting, stack for alert history. Ties conceptually into T.R.I.A.G.E's rescue-data pipeline. v2 (Arduino hardware + PCB) will be a separate, later repo.